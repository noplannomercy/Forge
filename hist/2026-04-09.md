# 2026-04-09 작업 요약

## 완료

| 작업 | 내용 |
|------|------|
| 프롬프트 외부화 | 6 Task — DB 저장, 버전 관리, API, 캐시, worker 연결 |
| semantic v2 프롬프트 | POST /prompts로 등록, +22.7% 품질 향상 (52k→64k chars) |
| callback URL | 2 Task — 완료/실패 시 POST + 3회 retry |
| TODO + CLAUDE.md 현행화 | 매 작업 후 동기화 |

## 테스트

- Unit tests: 139 passed
- 프롬프트 v2 before/after: 안산시 PDF 52,038자 → 63,835자

## Cortex 연동 완료

| # | 항목 | 결과 |
|---|------|------|
| 1 | Forge 변환 | 거버 제안.docx → 마크다운 |
| 2 | callback → Cortex ingest | pre_converted=true + X-API-Key |
| 3 | 청크 저장 | 11 chunks |
| 4 | 메타데이터 | 9개 키 (Forge meta + Cortex LLM merge) |
| 5 | file_name | 원본명 보존 |
| 6 | X-API-Key | 인증 통과 |

버그 수정:
- callback_url을 DB가 아닌 메모리에서 전달 (PostgresJobStore에 컬럼 없음)
- CALLBACK_API_KEY 환경변수 추가 (Cortex X-API-Key 전달)
- pre_converted:true 플래그 추가 (Cortex 텍스트 모드)

## 추가 완료

| 작업 | 내용 |
|------|------|
| HWPX extractor | 텍스트+표 추출, 실제 파일 179K자 + 1,348표 |
| HWPX 리뷰 수정 | confidence 주석, exception 로깅 |
| Cortex 연계 테스트 | callback → ingest → 11 chunks 성공 |
| callback payload 수정 | Cortex 호환 (content, pre_converted, X-API-Key) |
| Redis 보류 결정 | PostgreSQL 영속화 충분, Cortex Redis와 독립 |
| README.md | 퀵스타트, API 사용법, 환경변수, Docker, Cortex 연동 |

## Docker 통합 환경 구축 (저녁 추가)

| # | 항목 | 결과 |
|---|------|------|
| 1 | 하드코딩 전수 조사 | `.env.example` 정리, `domain` 쿼리파라미터화, schema.sql startup auto-apply |
| 2 | Dockerfile 보강 | libreoffice-impress, non-root user, healthcheck, curl |
| 3 | `.dockerignore` 전면 보강 | 테스트/문서/비밀/빌드 아티팩트 제외 |
| 4 | `docker-compose.yml` (단독) | 외부 DB 가정 |
| 5 | `docker-compose.integration.yml` | `hc-rag-network` 참조, DATABASE_URL override |
| 6 | Forge 도커 E2E 테스트 | DOCX/XLSX/callback/배치/에러/DB/스키마 auto-apply 12개 항목 전부 ✅ |
| 7 | Cortex 연계 합의 | 포트 8003 확정, 네트워크 이름 `hc-rag-network`, infra/cortex/forge 3-compose 구조 |
| 8 | 통합 테스트 실행 | network→infra→cortex→forge 순서 기동, 전부 healthy |
| 9 | End-to-end 플로우 | 거버제안.docx (11 chunks) + 현대케피코.docx (14 chunks) = 25 docs / 222 entities / 210 relations |
| 10 | 매뉴얼 작성 | `docs/DEPLOYMENT.md` — 아키텍처/환경별 실행/환경변수/URL 매트릭스/AWS 이관/트러블슈팅 |

주요 변경:
- `.env.example` — 플레이스홀더 정리
- `app.py` — `domain` 쿼리파라미터 + schema.sql auto-apply
- `worker.py` — domain 고정값 "general" 제거
- `models.py` — `Job.domain` 필드
- `Dockerfile`, `.dockerignore`, compose 2벌, DEPLOYMENT.md 신규

Cortex 팀과 합의된 내용:
- Postgres 이미지: `graphrag-test-postgres:latest` (pgvector+AGE 재사용)
- DB 이름/유저: `hc_rag` / `hc` / `hc_dev` (로컬 개발용)
- 스키마 관리: 양쪽 lifespan에서 startup auto-apply
- 네트워크: `hc-rag-network` (external)
- CALLBACK_API_KEY 기본값 `test` 양쪽 동일

## 남은 작업

| 우선순위 | 작업 | 상태 |
|----------|------|------|
| 1 | HWP(구형) 지원 | LibreOffice 경로, TODO |
| 2 | 품질 평가 API | 데이터 쌓인 후 |
| 3 | LLMOps API | 품질 평가 후 |
| 4 | S3/URL 기반 `/convert/url` 엔드포인트 | n8n 배치 파이프라인 도입 시 |
| 5 | AWS 이관 (ECS Fargate + RDS) | 프로덕션 진입 시 |
| defer | PG 통합 테스트, VLMClient singleton, MV REFRESH | 각 15분 |
